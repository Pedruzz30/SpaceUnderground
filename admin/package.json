{
  "name": "space-underground-admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test \"tests/*.test.mjs\"",
    "test:e2e": "node tests/e2e/admin-flow.mjs",
    "test:e2e:supabase": "node --env-file-if-exists=.env.local tests/e2e/supabase-flow.mjs",
    "test:e2e:media": "node --env-file-if-exists=.env.local tests/e2e/supabase-media-flow.mjs"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "playwright": "^1.63.0",
    "vite": "^7.1.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0"
  }
}
