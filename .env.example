# Public site — read-only access to Supabase.
# Both values are public: the publishable key only ever sees what row level
# security allows (published, visible projects). NEVER use the service role key.
# Leave them empty to keep the site on its bundled project registry.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
